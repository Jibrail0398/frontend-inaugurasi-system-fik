Options -MultiViews
RewriteEngine On
RewriteBase /

# Jika file atau folder fisik tidak ada, redirect ke index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
