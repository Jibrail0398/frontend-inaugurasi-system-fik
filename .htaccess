Options -MultiViews
RewriteEngine On
RewriteBase /

# Abaikan file/folder fisik yang memang ada
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Kalau tidak ada file/folder fisik, arahkan semua ke index.html
RewriteRule ^ index.html [L]
