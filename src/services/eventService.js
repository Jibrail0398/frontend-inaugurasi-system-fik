export const getEvents = () => {
    return [];
};

export const getEventById = (id) => {
    return {};
};

export const createEvent = ({ title, date, description }) => {
    return {};
};

export const updateEvent = ({ id, title, date, description }) => {
    return {};
};

export const deleteEvent = (id) => {
    return {};
};
